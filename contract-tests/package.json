{
  "name": "@platform/contract-tests",
  "version": "1.0.0",
  "description": "Contract tests for tab compliance with platform expectations",
  "main": "src/index.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "generate:schema": "ts-json-schema-generator --path ../content-platform/tab-contract/src/index.ts --type TabModule --out schemas/TabModule.schema.json"
  },
  "keywords": ["contract-testing", "schema", "validation"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "ajv": "^8.12.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-json-schema-generator": "^1.5.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@content-platform/tab-contract": "*"
  }
}
